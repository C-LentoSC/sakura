// Petal animation type
export interface PetalAnimation {
  x: string;
  y: string;
  delay: number;
  duration: number;
}

// Feature item type
export interface Feature {
  icon: string;
  title: string;
  description: string;
  iconPath: string;
}
